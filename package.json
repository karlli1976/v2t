{
  "name": "v2t",
  "version": "0.1.0",
  "description": "Extract transcripts from video URLs",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "files": ["dist/"],
  "bin": {
    "v2t": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
